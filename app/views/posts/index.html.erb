<% @posts.each do |post| %>
   <%= render 'post', :post => post %>
<% end %>

<br />

<%= will_paginate @posts, :inner_window => 1, :outer_window => 0, :renderer => BootstrapPaginationHelper::LinkRenderer %>
